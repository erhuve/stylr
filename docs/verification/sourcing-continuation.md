# Sourcing continuation, September 16, 2026

Two immutable batches contain 260 directly inspected candidates from 12 successful sources. Their 22 contact sheets and 20 enlarged contour rereads produce 139 admitted photographs across 112 new source pages, 124 build observations and 12 complete three-axis references. Three initially proposed complete records lost unsupported axes after enlargement. A further enlarged color inspection confirmed the black hooded coat at street ordinal 98 retains skin color; its unchanged original joins the narrow, ID-specific low-chroma test exceptions (minimum 0.5). No image was recolored.

The catalog contains 1,707 photos, 1,466 review records, 1,248 build observations and 195 complete references. The build 3 / shoulder 0 / waist 1 nearby example returns 33 matches, formerly 30. Matching tolerances and unknown handling are unchanged. All prior 1,568 records retain exact metadata, hashes, body labels and order.

The source inventory indexes 1,312 provably sampled pages across 24 normalized source labels and nine historical/current run manifests. The new runs record 2,734 listing-product decisions, including 131 selected pages; 1,117 distinct observed eligible pages remain deferred after removing sampled pages and applying the latest recorded decisions. Those are a concrete next-search queue, not reviewed photos. Earlier missing skipped-page decisions are explicitly unknown. Failed Forest Ink retrieval is preserved; its next attempt succeeded. Two exact duplicate image downloads were excluded before visual review.

## Verification

- Typecheck and all 126 unit tests pass. The new sourcing regression suite checks committed-history deduplication, every listing-decision class, exact image duplicates, durable network errors and preservation of interrupted runs. Existing Python admission/restore regressions pass.
- Admission replay, 260 candidate image bindings, all 1,707 asset digests and all 42 original-photo visual identities pass.
- The initial browser invocation needed the already-installed Chromium selected explicitly. With that configuration, 85 browser tests pass; the low-chroma check identified the coat above. After the narrow reviewed exception, the specific color/asset-loading rerun passes too: all 86 browser cases pass across these verification runs.
- Git prefix comparison verifies the complete old photo, asset and body-reference arrays remain unchanged.

Deployment pending verification. Scheduled September 17–23 sourcing runs commit verified progress but do not automatically deploy.
